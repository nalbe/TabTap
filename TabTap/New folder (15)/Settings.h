#ifndef SETTINGS_WINDOW_H
#define SETTINGS_WINDOW_H

// Displays a settings window with checkboxes and buttons.
// This function runs its own message loop until the window is closed.
void ShowSettingsWindow(HINSTANCE hInstance);

#endif // SETTINGS_WINDOW_H